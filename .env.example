# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Better Auth Configuration
BETTER_AUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000

# MongoDB Connection
MONGODB_HOST=mongodb+srv://username:password@cluster.mongodb.net/database?appName=mymonster

# GitHub OAuth (for authentication)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# Notes:
# - BETTER_AUTH_SECRET: Generate with `openssl rand -base64 32`
# - MONGODB_HOST: Get from MongoDB Atlas connection string
# - GITHUB_CLIENT_ID/SECRET: Create OAuth App at https://github.com/settings/developers
# - STRIPE keys: Get from https://dashboard.stripe.com/test/apikeys
# - STRIPE_WEBHOOK_SECRET (local): Run `stripe listen` to get the secret
# - STRIPE_WEBHOOK_SECRET (prod): Create webhook in Stripe Dashboard
